[core]
id = "dmg:cpu:lsu_harden"
filesets = ["rtl", "sim_model"]

[fileset.rtl]
dir = "lsu"
files = ["lsu_harden_top.sv"]

[fileset.sim_model]
dir = "lsu"
files = [
  "!is_synth && !is_lint ? (model/sram_sim_model.sv)",
  "!is_synth && !is_lint ? (model/dw_sim_model.sv)",
]
