[core]
id = "dmg:cpu:lsu_harden"
filesets = ["rtl", "sram_sim_model", "dw_sim_model"]

[fileset.rtl]
dir = "lsu"
files = ["lsu_harden_top.sv"]

[fileset.sram_sim_model]
legacy_f = "!is_synth && !is_lint ? (sram_sim_model.f)"

[fileset.dw_sim_model]
depend = [
  "!is_synth && !is_lint ? (dmg:cpu:dw_sim_model)",
]
