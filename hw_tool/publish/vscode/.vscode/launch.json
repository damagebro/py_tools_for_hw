{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run HW Tool VS Code Extension",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ]
        }
    ]
}
